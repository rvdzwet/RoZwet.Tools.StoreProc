## Build output
bin/
obj/

## Secrets — never commit
appsettings.json

## Runtime state — never commit
checkpoint.json
checkpoint.json.tmp

## VS artifacts
.vs/
*.user
*.suo

## Temporary reflection probe (not part of production code)
probe/

## OS
.DS_Store
Thumbs.db
